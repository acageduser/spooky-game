// Draw GUI Event in objDialogueManager

//handles drawing the dialogue box when active
if (global.isDialogueActive && global.dialogueBoxInstance) {
    global.dialogueBoxInstance.draw();  //draw the dialogue box and text
}
