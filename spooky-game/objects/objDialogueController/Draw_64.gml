/// @function Draw GUI
/// @desc calls the draw method for rendering the dialogue box and text
/// @return none

if (global.dialogueBoxInstance != undefined) {
    global.dialogueBoxInstance.draw();  //draw dialogue box and content
}
